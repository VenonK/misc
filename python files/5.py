numOfDays = int(input("what is the number of days you want to convert" + "\n"))
numOfHours = numOfDays * 24
numOfMinutes = numOfHours * 60
numOfSeconds = numOfMinutes * 60
print(f"in {numOfDays} days, there is {numOfHours} hours, {numOfMinutes} minutes and {numOfSeconds} seconds")